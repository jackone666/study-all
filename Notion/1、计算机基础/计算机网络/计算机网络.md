---
notion-id: 31fa95f7-5835-8070-9d22-dd65ccb8ef7f
---

## **基 础**

### **2.说 ⼀ 下 每 ⼀ 层 对 应 的 ⽹ 络 协 议 有 哪 些 ？**
![[image 1.png]]
### **3.那 么 数 据 在 各 层 之 间 是 怎 么 传 输 的 呢 ？**
对 于 发 送 ⽅ ⽽ ⾔， 从 上 层 到 下 层 层 层 包 装 ， 对 于 接 收 ⽅ ⽽ ⾔， 从 下 层 到 上 层 ， 层 层 解 开 包 装。
### **4.从浏览器地址栏输入 URL 到显示主页的过程？**
1、DNS 解析：将域名解析成对应的 IP 地址。
2、TCP 连接：与服务器通过三次握手，建立连接并向服务器发送 HTTP 请求。
3、服务器处理请求，返回 HTTP 响应。
4、浏览器解析并渲染页面。
5、断开连接：TCP 四次挥手，连接结束。
### 各个过程都使用了哪些协议？
![[image 2.png]]
### **5.说 说 DNS 的 解 析 过 程 ？**
![[image 3.png]]
**假设你要查询 **[**www.baidu.com**](http://www.baidu.com/)** 的 IP 地址：**
1. 首先会查找浏览器的缓存，看看是否能找到 [**www.baidu.com**](http://www.baidu.com/) 对应的 IP，则进行下一步。
2. 将请求发往本地 DNS 服务器，如果查找到也直接返回，否则继续进行下一步。
3. 本地 DNS 服务器向 **根域名服务器** 发送请求，根域名服务器返回负责 **com** 的顶级域名服务器的 IP 地址列表。
4. 本地 DNS 服务器再向其中一个负责 **com** 的顶级域名服务器发送一个请求，返回负责 [**baidu.com**](http://baidu.com/) 的权限域名服务器的 IP 地址列表。
5. 本地 DNS 服务器再向其中一个权限域名服务器发送一个请求，返回 [**www.baidu.com**](http://www.baidu.com/) 所对应的 IP 地址。

### **6.WebSocket 与 Socket 的 区 别 ？**
- Socket 其实等于 **IP 地址 + 端口 + 协议**。具体来说，Socket 更好地进行网络编程。**Socket 是一套标准，它完成了对 TCP/IP 的高度封装，屏蔽网络细节，方便开发者。**
- WebSocket 是为解决 **HTTP 不支持持久化连接** 而出现的协议，属于应用层通信协议。

Socket 是一个持久化的协议接口，是伴随 H5 出现的 **网编编程标准接口**。
### **7.说 ⼀ 下 你 了 解 的 端 ⼜ 及 对 应 的 服 务 ？**
![[image 4.png]]
### **8.说 说 HTTP 常 ⽤ 的 状 态 码 及 其 含 义**
HTTP 状态码可以先按类别记：
- **1xx**：信息性状态码
- **2xx**：成功状态码
- **3xx**：重定向状态码
- **4xx**：客户端错误状态码
- **5xx**：服务端错误状态码

常用状态码（面试和实际排查都常见）：
![[image 5.png]]
### **说一下 301 和 302 的区别？**
- **301（永久重定向）**：请求的资源已被永久移动到新位置。服务器会在响应中返回新的资源地址（Location）。
- **302（临时重定向）**：服务器临时从另一个地址响应资源，但客户端后续仍应使用原地址。

### **9.HTTP 有 哪 些 请 求 ⽅ 式 ？**
P O S T 、 D E L E T E 、 P U T 、 G E T 的 含 义 分 别 对 应 我 们 最 熟 悉 的 增、 删、 改、 查。
### **10.说 ⼀ 下 GET 和 POST 的 区 别 ？**
![[image 6.png]]
### 从 HTTP 报文层面
- **数据位置**：GET 将信息放在 URL 中；POST 将请求信息放在请求体中。
- **数据大小**：GET 携带的数据量有限，因为 URL 本身有长度限制；POST 的数据存放在报文体中，因此对大小通常没有限制。
- **安全性（形式上）**：GET 把数据放在 URL 上不太安全；POST 把数据放在请求体里，相对更安全一些。

### 从数据库（语义）层面
- **幂等性与安全性**：GET 符合幂等性和安全性；POST 请求不符合。
- **原因**：按照 HTTP 的约定，GET 用于查看信息，不会改变服务器上的信息；POST 用来改变服务器上的信息。
- **解释**：GET 只查看不改变，对数据库一次或多次操作获得的结果一致，因此认为它符合幂等性；安全性是指对数据库操作不会改变数据库中的数据。

## 缓存层面
- **缓存**：GET 请求能够被缓存。
- **浏览记录**：GET 请求能够保存在浏览器的浏览记录里。
- **书签**：GET 请求的 URL 能够保存为浏览器书签。
- **CDN（本质上缓存：caffine→redis→cnd 三级缓存）**：缓存是 GET 请求被广泛应用的根本。因为它的幂等性和安全性，除了返回结果没有其他多余动作，因此绝大部分 GET 请求都会被 CDN 缓存，从而大大减少 Web 服务器的负担。

### **12.HTTP 请 求的过 程与原 理 ？**
6. 每个服务器都有一个进程，它不断监听 TCP 端口 80，以便发现是否有浏览器向它发出连接建立请求。
7. 监听到连接请求后，就会建立 TCP 连接。
8. 浏览器向服务器发出浏览某个页面的请求，服务器返回所请求的页面作为响应。
9. 最后，释放 TCP 连接。

### **13.说 ⼀ 下 HTTP 的 报 ⽂ 结 构 ？**
H T T P 报 ⽂ 有 两 种 ， H T T P 请 求 报 ⽂ 和 H T T P 响 应 报 ⽂ ：
### **14.URI 和 URL 有 什 么 区 别 ?**
URI（统一资源标识符，Uniform Resource Identifier）用于标识 Web 上的每一种可用资源，例如 HTML 文档、图像、视频片段、程序等。
URL（统一资源定位符，Uniform Resource Locator）是 URI 的一个子集，主要用于提供资源的访问路径。
### **15.说 下 HTTP/1.0， 1.1， 2.0 的 区 别 ？**
关键需要记住：**HTTP/1.0** 默认是短连接，可以强制开启；**HTTP/1.1** 默认长连接；**HTTP/2.0** 采用多路复用。
### **HTTP/1.0**
- 默认使用**短连接**，每次请求都需要建立一个 TCP 连接。它可以设置 `Connection: keep-alive` 这个字段，强制开启长连接。

### **HTTP/1.1**
- **引入了持久连接**，即 TCP 连接默认不关闭，可以被多个请求复用。
- **分块传输编码**，即服务端每产生一块数据，就发送一块，用“流模式”取代“缓存模式”。
- **管道机制**，即在同一个 TCP 连接里面，客户端可以同时发送多个请求。

### **HTTP/2.0**
- **二进制协议**：1.1 版本的头信息是文本（ASCII 编码），数据体可以是文本或者二进制；2.0 中，头信息和数据体都是二进制。
- **完全多路复用**：在一个连接里，客户端和浏览器都可以同时发送多个请求或回应，而且不用按照顺序一一对应。
- **报头压缩**：HTTP 协议不带有状态，每次请求都必须附上所有信息。HTTP/2.0 引入了头信息压缩机制，使用 gzip 或 compress 压缩后再发送。
- **服务端推送**：允许服务器未经请求，主动向客户端发送资源。

### **16.HTTP/3了解吗 ？**
**HTTP/3** 主要有两大变化：**传输层基于 UDP**、**使用 QUIC 保证 UDP 可靠性**。HTTP/2 存在的一些问题，比如重传等等，都是由于 TCP 本身的特性导致的，所以 HTTP/3 在 QUIC 的基础上进行发展而来。QUIC（Quick UDP Connections）直译为**快速 UDP 网络连接**，底层使用 UDP 进行数据传输。
### **HTTP/3 主要有这些特点：**
- **使用 UDP 作为传输层进行通信**。
- **安全性保证**：在 UDP 的基础上，QUIC 协议保证了 HTTP/3 的安全性，在传输的过程中就完成了 **TLS 加密握手**。
- **减少交互次数**：HTTPS 要建立一个连接，要花费 6 次交互（先是 TCP 三次握手，然后是 TLS/1.3 的三次握手）。QUIC 直接把以往的 TCP 和 TLS/1.3 的 **6 次交互合并成了 3 次**，减少了往返延迟。
- **解决行头阻塞（Head-of-Line Blocking）**：QUIC 有自己的一套机制可以保证传输的可靠性。当某个“流”发生丢包时，**只会阻塞这个流**，其他流不会受到影响（解决了 TCP 层面丢包导致的所有流阻塞问题）。

### **17.HTTP如何实现长连接？在什么时候会超时？**
### **什么是HTTP的长连接 ？**
10. HTTP 分为长连接和短连接，本质上说的是 TCP 的长短连接。TCP 连接是一个双向通道，可以保持一段时间不关闭，因此 TCP 连接才具有真正的长连接和短连接之分。
11. TCP 长连接可以复用一个 TCP 连接来发起多次 HTTP 请求，从而减少资源消耗。比如一次请求 HTML，如果是短连接，可能还需要为后续的 JS 和 CSS 再建立连接。

### **如何设置长连接？**
通过在头部（请求和响应头）设置 `Connection` 字段指定为 `keep-alive`。HTTP/1.0 协议支持长连接，但是默认关闭的；从 **HTTP/1.1 以后，连接默认都是长连接**。
### **在什么时候会超时呢？**
- **HTTP 层面**：
一般会有 `httpd` 守护进程，里面可以设置 `keep-alive timeout`。当 TCP 连接闲置超过这个时间就会关闭，也可以在 HTTP 的 header 里面设置超时时间。
- **TCP 层面**：
TCP 的 `keep-alive` 包含三个参数，支持在系统内核的 `net.ipv4` 里面设置。当 TCP 连接建立之后：
    1. **tcp_keepalive_time**：闲置多久后开始发送探测包（图中设置为 1800 秒）。
    2. **tcp_keepalive_intvl**：如果没有收到对方的 ACK，每隔多久再发一次探测包（图中设置为 15 秒）。
    3. **tcp_keepalive_probes**：连续探测多少次失败后，就会丢弃该连接（图中设置为 5 次）。

### **18.说 说 HTTP 与 HTTPS 有 哪 些 区 别 ？**
- **安全性**：
HTTP 是超文本传输协议，信息是**明文传输**，存在安全风险的问题。HTTPS 则解决了 HTTP 不安全的缺陷，在 TCP 和 HTTP 网络层之间加入了 **SSL/TLS 安全协议**，使得报文能够加密传输。
- **连接建立过程**：
HTTP 连接建立相对简单，**TCP 三次握手**之后便可进行 HTTP 的报文传输。而 HTTPS 在 TCP 三次握手之后，还需进行 **SSL/TLS 的握手过程**，才可进入加密报文传输。
- **端口号**：
HTTP 的默认端口号是 **80**，HTTPS 的默认端口号是 **443**。
- **证书体系**：
HTTPS 协议需要向 **CA（证书权威机构）**申请数字证书，来保证服务器的身分是可信的。

### **19.为什么要用 HTTPS？解决了哪些问题？**
因为 HTTP 是明文传输，存在安全上的风险：
**窃听风险**，比如通信链路上可以获取通信内容，用户账号被盗。
**篡改风险**，比如强制植入垃圾广告，视觉污染。
**冒充风险**，比如冒充淘宝网站，用户金钱损失
**所以引入了 HTTPS，HTTPS 在 HTTP 与 TCP 层之间加入了 SSL/TLS 协议，可以很好地解决了这些风险：**
- **信息加密**：交互信息无法被窃取（防窃听）。
- **校验机制**：无法篡改通信内容，篡改了就不能正常显示（防篡改，通常使用数字签名/摘要算法）。
- **身份证书**：能证明淘宝是真淘宝（防冒充）。

所以 **SSL/TLS 协议是能保证通信是安全的**。
### **20.HTTPS 工作流程是怎样的？**
这道题有几个要点：**公私钥、数字证书、加密、对称加密、非对称加密**。
HTTPS 主要工作流程：
- **客户端发起 HTTPS 请求**，连接到服务端的 443 端口。
- **服务端有一套数字证书**（证书内容有公钥、证书颁发机构、失效日期等）。
- **服务端将自己的数字证书发送给客户端**（公钥在证书里面，私钥由服务器持有）。
- **客户端收到数字证书之后，会验证证书的合法性**。如果证书验证通过，就会生成一个随机的对称密钥，用证书的公钥加密。
- **客户端将公钥加密后的密钥发送到服务器**。
- **服务器接收到客户端发来的密文密钥之后**，用自己之前保留的私钥对其进行非对称解密，解密之后就得到客户端的密钥，然后用客户端密钥对返回数据进行对称加密，酱紫传输的数据都是密文啦。
- **服务器将加密后的密文返回到客户端**。
- **客户端收到后，用自己的密钥对其进行对称解密**，得到服务器返回的数据。

### **21. 客户端怎么去校验证书的合法性？**
**首先，服务端的证书从哪来的呢？**
为了让服务端的公钥被大家信任，服务端的证书都是由 **CA**（**Certificate Authority**，证书认证机构）签名的，CA 就是网络世界里的公安局、公证中心，具有极高的可信度，所以由它来给各个公钥签名，信任的一方签发的证书，那必然证书也是被信任的。
![[image 7.png]]
**CA 签发证书的过程，如上图左边部分：**
- 首先 CA 会把持有者的公钥、用途、颁发者、有效时间等信息打成一个包，然后对这些信息进行 **Hash 计算**，得到一个 **Hash 值**；
- 然后 CA 会使用自己的**私钥**将该 Hash 值加密，生成 **Certificate Signature**，也就是 CA 对证书做了签名；
- 最后将 Certificate Signature 添加在文件证书上，形成**数字证书**；

**客户端校验服务端的数字证书的过程，如上图右边部分：**
- 首先客户端会使用同样的 **Hash 算法**获取该证书的 **Hash 值 H1**；
- 通常浏览器和操作系统中集成了 CA 的公钥信息，浏览器收到证书后可以使用 **CA 的公钥**解密 **Certificate Signature 内容**，得到一个 **Hash 值 H2**；
- 最后比较 **H1 和 H2**，如果值相同，则为可信赖的证书，否则则认为证书不可信。

**假如在 HTTPS 的通信过程中，中间人篡改了证书原文，由于他没有 CA 机构的私钥，所以 CA 公钥解密的内容就不一致。**
### **非对称加密—“互为加解密”的两种场景**
非对称加密最神奇的地方在于它的**双向性**。根据谁负责加密，应用场景截然不同：
### **A. 公钥加密 -> 私钥解密（确保私密性）**
这是最常见的场景，用于发送只有对方能看懂的信息。
- **流程：** 我用你的**公钥**给文件加锁 -> 文件传输 -> 你用你的**私钥**开锁。
- **结果：** 只有拥有私钥的你能看到内容。即便文件在传输过程中被截获，黑客因为没有私钥也打不开。

### **B. 私钥加密 -> 公钥解密（确保真实性/数字签名）**
这通常被称为**“数字签名”**。
- **流程：** 我用我的**私钥**对一段数据的哈希值进行加密（签名） -> 发送给你 -> 你用我的**公钥**解密。
- **结果：** 如果你能用公钥成功解密，说明这段信息**确实是我发的**（身份认证），且信息**没有被篡改过**（完整性）。

### **22. 如何理解 HTTP 协议是无状态的？**
这个 **无状态** 的 **状态** 指的是什么？是客户端的状态，所以字面意思，就是 HTTP 协议中服务端不会保存客户端的任何信息。
比如当浏览器第一次发送请求给服务器时，服务器响应了；如果同一个浏览器发起第二次请求给服务器时，它还是会响应，但是呢，服务器不知道你就是刚才的那个浏览器。
**常用的保持状态Cookie 和 Session**
### **23.说 说 Session 和 Cookie 有 什 么 联 系 和 区 别 ?**
### **先来看看什么是 Session 和 Cookie：**
- **Cookie** 是保存在客户端的一小块文本串的数据。客户端向服务器发起请求时，服务端会向客户端发送一个 Cookie，客户端就把 Cookie 保存起来。在客户端下次向同一服务器再发起请求时，Cookie 被携带发送到服务器。服务端可以根据这个 Cookie 判断用户的身份和状态。
- **Session** 指的就是服务器和客户端一次会话的过程。它是另一种记录客户状态的机制。不同的是 cookie 保存在客户端浏览器中，而 session 保存在服务器上。客户端浏览器访问服务器的时候，服务器把客户端信息以某种形式记录在服务器上，这就是 session。客户端浏览器再次访问时只需要从该 session 中查找用户的状态

**Session 和 Cookie 到底有什么不同呢？**
- **存储位置不一样**，Cookie 保存在客户端，Session 保存在服务器端。
- **存储数据类型不一样**，Cookie 只能保存 ASCII，Session 可以存任意数据类型，一般情况下我们可以在 Session 中保持一些常用变量信息，比如说 UserId 等。
- **有效期不同**，Cookie 可设置为长时间保持，比如我们经常使用的默认登录功能，Session 一般有效时间较短，客户端关闭或者 Session 超时都会失效。
- **隐私策略不同**，Cookie 存储在客户端，比较容易遭到不法获取，早期有人将用户的登录名和密码存储在 Cookie 中导致信息被窃取；Session 存储在服务端，安全性相对 Cookie 要好一些。
- **存储大小不同**，单个 Cookie 保存的数据不能超过 4K，Session 可存储数据远高于 Cookie。

### **分布式环境下 Session 怎么处理呢？**
使用redis缓存
### **24.详细说一下TCP的三次握手机制**
TCP提供面向连接的服务，在传送数据前必须建立连接，TCP连接是通过三次握手建立的。
![[image 8.png]]
### **三次握手的过程：**
- **最开始**，客户端和服务端都处于 `CLOSE` 状态，服务端监听客户端的请求，进入 `LISTEN` 状态。
- **客户端发送连接请求**，第一次握手（`SYN=1, seq=x`），发送完毕后，客户端就进入 `SYN_SENT` 状态。
- **服务端确认连接**，第二次握手（`SYN=1, ACK=1, seq=y, ACKnum=x+1`），发送完毕后，服务端就进入 `SYN_RCV` 状态。
- **客户端收到服务端的确认之后**，再次向服务端确认，这就是 **第三次握手**（`ACK=1, ACKnum=y+1`），发送完毕后，客户端进入 `ESTABLISHED` 状态，当服务端接收到这个包时，也进入 `ESTABLISHED` 状态。

### **26.三次握手中每一次没收到报文会发生什么情况？**
- **第一次握手服务端未收到 SYN 报文**
服务端不会进行任何的动作，而客户端由于一段时间内没有收到服务端发来的确认报文，等待一段时间后会**重新发送 SYN 报文**，如果仍然没有回应，会重复这个过程，直到发送次数超过**最大重传次数限制**，就会返回连接建立失败。
- **第二次握手客户端未收到服务端响应的 ACK 报文**
客户端会继续重传，直到次数限制；而服务端此时会阻塞在 `accept()` 处，等待客户端发送 ACK 报文。
- **第三次握手服务端未收到客户端发送过来的 ACK 报文**
服务端同样会采用类似客户端的**超时重传机制**，如果重试次数超过限制，则 `accept()` 调用返回 -1，服务端建立连接失败；而此时客户端认为自己已经建立连接成功，因此开始向服务端发送数据，但是服务端的 `accept()` 系统调用已经返回，此时不在监听状态，因此服务端接收到客户端发送来的数据时会发送 **RST 报文**给客户端，消除客户端单方面建立连接的状态。

### **27. 第二次握手传回了 ACK，为什么还要传回 SYN？**
`ACK` 是为了告诉客户端传来的数据已经接收无误。
而传回 `SYN` 是为了告诉客户端，服务端响应的确实是客户端发送的报文。
### **28. 第 3 次握手可以携带数据吗？**
第 3 次握手是可以携带数据的。
此时客户端已经处于 `ESTABLISHED` 状态。对于客户端来说，它已经建立连接成功，并且确认服务端的接收和发送能力是正常的。
第一次握手不能携带数据是出于安全的考虑，因为如果允许携带数据，攻击者每次在 `SYN` 报文中携带大量数据，就会导致服务端消耗更多的时间和空间去处理这些报文，会造成 CPU 和内存的消耗。
### **29. 说说半连接队列和 SYN Flood 攻击的关系？**

- **半连接队列 (SYN Queue)**：
    - 当服务端收到客户端的 `SYN` 包时，会将该连接放入半连接队列。
    - 此时服务端处于 `SYN_RECV` 状态，并向客户端发送 `SYN+ACK`。
- **全连接队列 (Accept Queue)**：
    - 当服务端收到客户端最后的 `ACK` 确认包后，连接从半连接队列移出，进入全连接队列。
    - 此时服务端处于 `ESTABLISHED` 状态，等待应用程序调用 `accept()`。
- **SYN Flood 攻击的关系**：
    - **原理**：攻击者伪造大量不存在的 IP 地址向服务器发送 `SYN` 包。
    - **后果**：服务器发送 `SYN+ACK` 后无法收到回执（`ACK`），导致**半连接队列被这些“僵尸连接”占满**。
    - **影响**：正常的连接请求因为半连接队列溢出而被丢弃，导致拒绝服务（DoS）

**常用的防御手段：**
- **SYN Cookies**：不直接在队列中存状态，而是通过算法生成一个 Cookie 回传，验证通过后再分配资源。
- **增大队列容量**：调整内核参数。
- **减少重试次数**：缩短等待超时连接的时间。

**30.说 说 TCP 四 次 挥 ⼿ 的 过 程 ？**
### **30、TCP 四次挥手的过程？为什么是四次不是三次？**
TCP 是全双工通信，关闭连接时需**分别关闭两个方向的传输通道**，因此需要四次挥手：
### **（1）四次挥手详细过程**

| **挥手次数** | **发起方** | **报文标志** | **核心动作** | **状态变化** |
| --- | --- | --- | --- | --- |
| 第一次挥手 | 客户端 | FIN（结束） | 发送 FIN=1，SEQ=x（表示客户端无数据要发，请求关闭客户端→服务端的通道） | 客户端→**FIN_WAIT_1** |
| 第二次挥手 | 服务端 | ACK（确认） | 发送 ACK=1，确认号=x+1（确认收到 FIN，此时客户端→服务端通道半关闭） | 服务端→**CLOSE_WAIT**；客户端收到后→FIN_WAIT_2 |
| 第三次挥手 | 服务端 | FIN（结束） | 服务端数据发送完毕，发送 FIN=1，SEQ=y（请求关闭服务端→客户端的通道） | 服务端→**LAST_ACK** |
| 第四次挥手 | 客户端 | ACK（确认） | 发送 ACK=1，确认号=y+1（确认收到 FIN） | 客户端→**TIME_WAIT**；服务端收到后→CLOSED |

### **（2）为什么是四次不是三次？**
TCP 全双工特性决定了**关闭通道需要双向确认**。服务端收到客户端的 FIN 时，可能还有数据要发，因此需要先回复 ACK 确认关闭客户端通道，等数据发完再发 FIN 关闭自己的通道 —— 这两步无法合并，因此必须四次挥手。
### **（3）核心考点：TIME_WAIT 状态**
客户端第四次挥手后进入 TIME_WAIT 状态，持续 **2MSL**（MSL 是报文最大生存时间，默认 1-2 分钟）。
作用：① 确保最后一个 ACK 送达（服务端没收到会重发 FIN）；② 避免旧连接的报文干扰新连接（2MSL 足够让旧报文失效）。
### 32. TCP 四次挥手过程中，为什么需要等待 2MSL，才进入 CLOSED 关闭状态？
### 为什么需要等待？
12. **为了保证客户端发送的最后一个 ACK 报文段能够到达服务端**。
这个 ACK 报文段有可能丢失，从而使处在 **LAST-ACK** 状态的服务端就收不到对已发送的 **FIN + ACK** 报文段的确认。服务端会超时重传这个 FIN + ACK 报文段，而客户端就能在 2MSL 时间内（**超时 + 1MSL 传输**）收到这个重传的 FIN + ACK 报文段。接着客户端重传一次确认，重新启动 2MSL 计时器。最后，客户端和服务端都正常进入到 **CLOSED** 状态。
13. **防止已失效的连接请求报文段出现在本连接中**。
客户端在发送完最后一个 ACK 报文段后，再经过时间 2MSL，就可以使本连接持续的时间内所产生的所有报文段都从网络中消失。这样就可以使下一个连接中不会出现这种旧的连接请求报文段。

### 为什么等待的时间是 2MSL？
- **MSL** 是 **Maximum Segment Lifetime**（报文最大生存时间），它是任何报文在网络上存在的最长时间，超过这个时间报文将被丢弃。
- **TIME_WAIT** 等待 2 倍的 MSL，比较合理的解释是：网络中可能存在来自发送方的数据包，当这些发送方的数据包被接收方处理后会向对方发送响应，所以**一来一回需要等待 2 倍的时间**。

### **一、 TIME_WAIT 过多**

**状态定位**：出现在**主动关闭连接的一方**（通常是客户端，但在爬虫或微服务调用中，服务端也可能作为调用方主动关闭）。
### 1. 产生原因
- **短连接频繁**：使用了大量的短连接（HTTP Short Connection），每次请求完成后立即关闭连接。
- **高并发压力**：在极短时间内产生大量连接，虽然每个连接都正常关闭，但由于 TCP 协议规定必须等待 **2MSL** 才能释放端口，导致状态堆积。
- **未开启连接池**：在微服务架构中，如果下游服务调用没有使用连接池，每次访问都会经历“建立-关闭”，迅速占满 65535 个端口。

### 2. 危害
- **端口耗尽**：新的连接请求因无法分配到可用端口而失败（Address already in use）。
- **资源占用**：虽然内存占用相对较小，但大量 socket 结构体会消耗系统内核资源。

### 3. 解决对策
- **启用 Keep-Alive**：改短连接为长连接，减少关闭频率

### 二、 CLOSE_WAIT 过多
**状态定位**：出现在**被动关闭连接的一方**（通常是服务端）。
### 1. 产生原因
**核心结论：这是代码 Bug，不是系统参数问题。**
当对方（客户端）发送了 FIN 包请求关闭连接时，你的服务器内核回了 ACK。此时，内核在等**应用程序**调用 `socket.close()` 来发送最后的 FIN 包。如果程序没关，就一直卡在 CLOSE_WAIT。
- **资源未释放**：代码里忘记在 `finally` 块或异常处理中调用 `close()`。
- **响应过慢/线程池满了**：程序忙于处理其他逻辑，或者线程池排队太长，导致负责关闭连接的逻辑一直没执行。
- **死锁或阻塞**：程序在关闭连接前触发了某个阻塞操作（如等待数据库锁），导致连接一直悬挂。

### 3. 解决对策
- **检查代码**：排查所有的 `Socket`、`HttpClient`、`JDBC Connection` 等资源是否都有正确的关闭逻辑。

| **状态** | **谁会出现** | **主要原因** | **解决核心** |
| --- | --- | --- | --- |
| **TIME_WAIT** | 主动关闭方 | 短连接过多、并发过高 | 优化内核参数、使用长连接 |
| **CLOSE_WAIT** | 被动关闭方 | **代码没写 close()**、程序阻塞 | **修复代码 Bug** |

### 33. 保活计时器有什么用？
**核心内容识别：**
- **设计初衷**：防止服务器在客户端发生故障（如突然掉电、崩溃）后，无休止地等待下去，白白浪费资源。
- **工作机制**：
    1. 服务器每收到一次数据，就重置计时器。
    2. 默认设置通常为 **2 小时**。
    3. 若 2 小时内没收到新数据，服务器会发送一个**探测报文段**。
    4. 随后每隔 **75 秒** 发送一次。
    5. 若连续发送 **10 个** 探测报文段仍无响应，服务器认为客户端故障，主动关闭连接

![[image 9.png]]
### 31. TCP 挥手为什么需要四次呢？
**核心逻辑解析：**
- **客户端的意图**：当客户端发送 `FIN` 包时，它只是在告诉服务端：“我的数据发完了，我不再发送新数据了”。但是，此时客户端**仍然具有接收数据**的能力。
- **服务端的反应**：
    1. 服务端收到 `FIN` 后，先回一个 `ACK`。这表示“我知道你没数据发了”。
    2. **关键点**：此时服务端可能还有没处理完的数据要发给客户端。它需要先把手头的工作做完。
    3. 当服务端也准备好关闭连接时，它才会发送自己的 `FIN` 包，表示“我也没数据要发了，同意关闭”。
- **结论**：由于服务端的 `ACK`（收到确认）和 `FIN`（准备好关闭）通常不在同一个时刻发生，所以这两个包必须分开发送。这就导致了比三次握手多出了一次。

### 34.CLOSE-WAIT 和 TIME-WAIT 的状态和意义？
### **CLOSE-WAIT 状态有什么意义？**
服务端收到客户端关闭连接的请求并确认之后，就会进入 CLOSE-WAIT 状态。此时服务端可能还有一些数据没有传输完成，因此不能立即关闭连接，而 CLOSE-WAIT 状态就是为了保证服务端在关闭连接之前将待发送的数据处理完。
### **TIME-WAIT 有什么意义？**

TIME-WAIT 状态发生在第四次挥手，当客户端向服务端发送 ACK 确认报文后进入 TIME-WAIT 状态。
### 1. 防止旧连接的数据包
如果客户端收到服务端的 FIN 报文之后立即关闭连接，但是此时服务端对应的端口并没有关闭，如果客户端在相同端口建立新的连接，可能会导致新连接收到旧连接残留的数据包，导致不可预料的异常发生。
### 2. 保证连接正确关闭
假设客户端最后一次发送的 ACK 包在传输的时候丢失了，由于 TCP 协议的超时重传机制，服务端将重发 FIN 报文，如果客户端没有维持 TIME-WAIT 状态而直接关闭的话，当收到服务端重新发送的 FIN 包时，客户端就会使用 RST 包来响应服务端，导致服务端以为有错误发生，然而实际关闭连接过程是正常的。
### 35. TIME_WAIT 状态过多会导致什么问题？怎么解决？
**TIME_WAIT 状态过多会导致什么问题？**
如果服务器有处于 TIME-WAIT 状态的 TCP，则说明是由服务器方主动发起的断开请求。过多的 TIME-WAIT 状态主要的危害有两种：
14. **内存资源占用**；
15. **对端口资源的占用**，一个 TCP 连接至少消耗一个本地端口；

---
**怎么解决 TIME_WAIT 状态过多？**
- 服务器可以设置 `SO_REUSEADDR` 套接字来通知内核，如果端口被占用，但是 TCP 连接位于 TIME_WAIT 状态时可以重用端口。
- 还可以使用**长连接**的方式来减少 TCP 的连接和断开，在长连接的业务里往往不需要考虑 TIME_WAIT 状态。

### **37.TCP 是 如 何 保 证 可 靠 性 的 ？**
**TCP 主要提供 连接管理、校验和、序列号/确认应答、超时重传、最大消息长度、滑动窗口控制等方法实现了可靠性传输。**
### 1. 连接管理
TCP 使用**三次握手**和**四次挥手**保证可靠地建立连接和释放连接，这里就不用多说了。
### 2. 校验和
TCP 将保持它首部和数据的校验和。这是一个**端到端**的校验和，目的是检测数据在传输过程中的任何变化。如果接收端的校验和有差错，TCP 将丢弃这个报文段和不确认收到此报文段。
### **3、序列号/确认应答 (Sequence Number & ACK)**：
TCP 为每个字节都编号。接收方收到后回复一个 ACK（确认号），告诉发送方：“我收到了第 X 字节之前的所有数据，下次请发第 X 字节开始的内容”，解决**丢包**和**乱序**问题。
### 4. **流量控制**：
TCP 连接的每一方都有固定大小的缓冲空间，TCP 的接收端只允许发送端发送接收端缓冲区能接纳的数据。当接收方来不及处理发送方的数据，能提示发送方降低发送的速率，防止包丢失。TCP 使用的流量控制协议是**可变大小的滑动窗口协议**。（TCP 利用滑动窗口实现流量控制）
### 5. 最大消息长度 (MSS)
在建立 TCP 连接的时候，双方约定一个最大的长度（MSS）作为发送的单位，重传的时候也是以这个单位来进行重传。理想的情况下是该长度的数据刚好不被网络层分块。
### 6. **超时重传**：
超时重传是指发送出去的数据包到接收到确认包之间的时间，如果超过了这个时间会被认为是丢包了，需要重传。
### 7. **拥塞控制**：
如果网络非常拥堵，此时再发送数据就会加重网络负担，那么发送的数据段很可能超过了最大生存时间也没有到达接收方，就会产生丢包问题。为此 TCP 引入**慢启动**机制，先发出少量数据，就像探路一样，先摸清当前的网络拥堵状态后，再决定按照多大的速度传送数据。
### **39、TCP 的拥塞控制是怎么实现的？**
拥塞控制是 TCP 的核心难点，目的是**防止发送方速率过快导致网络拥塞**，核心是维护两个参数：**拥塞窗口（cwnd）** 和**慢开始门限（ssthresh）**。
### **（1）四大核心算法**

| **算法** | **触发条件** | **核心逻辑** |
| --- | --- | --- |
| **慢开始** | 连接建立 / 超时重传后 | cwnd 从 1 开始，每收到一个 ACK 就翻倍（指数增长），直到 cwnd=ssthresh，进入拥塞避免 |
| **拥塞避免** | cwnd >= ssthresh | cwnd 每经过一个 RTT 增加 1（线性增长），缓慢提升速率，避免拥塞 |
| **快速重传** | 收到 3 个重复 ACK | 无需等待超时，直接重传丢失的报文段（减少重传延迟） |
| **快速恢复** | 触发快速重传后 | 将 ssthresh 设为当前 cwnd 的一半，cwnd=ssthresh，直接进入拥塞避免（避免流量骤降） |

### **（2）特殊情况：超时重传（严重拥塞）**
若发送方超时未收到 ACK，判定为网络严重拥塞：
将 ssthresh 设为当前 cwnd 的一半；
cwnd 重置为 1，重新进入慢开始阶段。
### 42. 说说 TCP 的重传机制？
重传包括：**超时重传**、**快速重传**、**带选择确认的重传（SACK）**、**重复 SACK** 四种。
### 1. 超时重传 (Retransmission Timeout)
- **原理**：发送方设定定时器，到期未收到 ACK 则重传。
- **缺点**：时间难控制。设长了反应慢，设短了容易产生误判导致重复发送。

### 2. 快速重传 (Fast Retransmission)
- **原理**：不依赖时间，而是看 ACK。如果收到 **3 个连续相同的重复 ACK**，发送方立刻意识到丢包，直接重传。
- **缺点**：它只告诉发送方“丢了一个包”，但并不清楚丢了多少个。如果同时丢了包 2、包 3，发送方是只传包 2 还是把包 2 以后的全部传一遍？

### 3. **带选择确认的重传（SACK）**
为了解决应该重传多少个包的问题？TCP 提供了带选择确认的重传（即 SACK，Selective Acknowledgment）。
**SACK 机制**就是，在快速重传的基础上，接收方返回最近收到报文段的序列号范围，这样发送方就知道接收方哪些数据包是没收到的。这样就很清楚应该重传哪些数据包。
![[image 10.png]]
如上图中，发送方收到了三次同样的 ACK 确认报文，于是就会触发快速重发机制，通过 SACK 信息发现只有 200~299 这段数据丢失，则重发时，就只选择了这个 TCP 段进行重发。
### **4、重复 SACK (D-SACK)**
D-SACK，英文是 Duplicate SACK，是在 SACK 的基础上做了一些扩展，主要用来告诉发送方，有哪些数据包，自己重复接受了。
**DSACK 的目的是帮助发送方判断，是否发生了包失序、ACK 丢失、包重复或伪重传。让 TCP 可以更好的做网络流控。**
### 1. D-SACK 解决的核心痛点：伪重传 (Spurious Retransmission)
有时候网络只是变慢了（延迟），或者 ACK 包丢了，发送方等不及就重传了数据。
- **没有 D-SACK 时**：接收方收到重复包，直接丢弃，不给反馈。发送方依然以为网络丢包严重，从而猛减拥塞窗口（降低网速）。
- **有了 D-SACK 后**：接收方回一个 SACK 包，里面带上这个重复包的序列号。发送方一看：“噢！原来你已经收到了，是我重传早了。”

### 2. 它能帮发送方判断出三种情况：
- **ACK 丢了**：数据到了，但回传的确认丢了，导致误重传。
- **网络乱序**：原本以为包丢了，结果它只是走远路迟到了（先重传，后原件到）。
- **包重复**：网络设备（如路由器）出了 bug，同一个包发了两遍。

### **43、什么是 TCP 粘包 / 拆包？原因和解决方法？**
**粘包**：多个数据包被合并成一个大数据包发送，接收方无法区分边界；
**拆包**：一个大数据包被拆分成多个小数据包发送，接收方需要重组。
### **（1）产生原因**
TCP 是**字节流协议**，无消息边界；
发送方的 Nagle 算法（合并小数据包发送，提高效率）；
接收方的缓冲区大小限制（缓冲区未满时，不会立即向上层交付数据）。
### **（2）解决方法（核心是添加消息边界）**
**固定长度**：每个数据包长度固定，接收方按固定长度读取；
**添加分隔符**：在数据包末尾添加特殊分隔符（如\r\n）；
**消息头 + 消息体**：消息头包含数据包总长度，接收方先读长度再读数据（主流方案）。
### **44、TCP 与 UDP 的区别（面试重中之重）**
TCP 和 UDP 是传输层两大核心协议，区别可从**连接性、可靠性、效率**等维度记忆：

| **特性** | **TCP** | **UDP** |
| --- | --- | --- |
| 连接性 | 面向连接（三次握手建立，四次挥手关闭） | 无连接（直接发送数据包） |
| 可靠性 | 可靠传输（序列号、确认应答、超时重传、流量控制、拥塞控制） | 不可靠传输（尽最大努力交付，不保证送达和有序） |
| 状态性 | 有状态（维护连接状态、序列号、窗口大小） | 无状态（发送后不关心后续） |
| 传输形式 | 面向字节流（无消息边界，可拆分 / 合并数据） | 面向报文（保留消息边界，应用给多大就发多大） |
| 首部开销 | 20~60 字节（可变） | 固定 8 字节（极简） |
| 通信方式 | 仅支持单播 | 支持单播、多播、广播 |
| 适用场景 | 可靠性优先：文件传输（FTP）、网页（HTTP）、邮件（SMTP） | 实时性优先：音视频直播、在线游戏、DNS 查询 |

### **45、什么时候选择 TCP，什么时候选 UDP？**
选择的核心是**权衡可靠性和实时性**：
**选 TCP 的场景**：数据不能丢失、必须有序到达。例如：文件传输（丢包会导致文件损坏）、电商支付（数据错误会导致交易异常）、网页加载（资源缺失会导致页面错乱）。
**选 UDP 的场景**：允许少量丢包，但要求低延迟。例如：视频直播（丢几个帧不影响观看）、在线游戏（延迟高比丢包更致命）、DNS 查询（快比准更重要，丢包可重查）。
**特殊情况**：若需 “UDP 的实时性 + TCP 的可靠性”，可在应用层实现可靠机制（如 RTP 协议 + 重传机制）。
### **46、基于 TCP/UDP 的协议有哪些？**
传输层协议为应用层提供服务，需记住常见协议的底层依赖：
**基于 TCP 的协议**（可靠性优先）：HTTP/HTTPS（网页）、FTP（文件传输）、SMTP/POP3/IMAP（邮件）、SSH（远程登录）、Telnet。
**基于 UDP 的协议**（实时性优先）：DNS（域名解析）、DHCP（IP 地址分配）、RTP（音视频传输）、SNMP（网络管理）、TFTP（简单文件传输）。