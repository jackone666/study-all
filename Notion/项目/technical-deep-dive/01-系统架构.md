# Enterprise Hybrid RAG — 技术深度剖析文档

> 面向企业内部知识库/开发者支持/客服问答场景，基于 LangGraph 实现的规则驱动 + LLM 可选增强混合 RAG 系统。意图分类、工具选择等核心决策以关键词/正则匹配为主，LLM 调用仅在同步上下文且配置了真实 API key 时作为可选增强

---

## 目录

1. [系统技术架构](#1-系统技术架构)
2. [项目总览与设计哲学](#2-项目总览与设计哲学)
3. [核心架构：LangGraph 工作流](#3-核心架构langgraph-工作流)
4. [功能模块深度解析](#4-模块体系深度解析)
5. [RAG 检索引擎](#5-rag-检索引擎)
   - [5.8 ES IK Analyzer 中文分词方案详解](#58-es-ik-analyzer-中文分词方案详解)
   - [5.12 RAG 知识库更新策略（定时+实时）](#512-rag-知识库更新策略定时--实时)
   - [5.13 Graph-Augmented Hybrid RAG 🆕](#513-graph-augmented-hybrid-rag-)
   - [5.14 Graph RAG 问题回答](#514-graph-augmented-hybrid-rag--问题回答)
6. [多层级记忆系统](#6-多层级记忆系统)
7. [上下文管理与 Token 预算](#7-上下文管理与-token-预算)
8. [工具系统与安全策略](#8-工具系统与安全策略)
9. [故障恢复与兜底机制](#9-故障恢复与兜底机制)
10. [可观测性体系](#10-可观测性体系)
11. [评估体系与 Data Flywheel](#11-评估体系与-data-flywheel)
12. [LLM Provider 抽象层](#12-llm-provider-抽象层)
13. [配置管理](#13-配置管理)
14. [中间件系统](#14-中间件系统)
15. [前端架构](#15-前端架构)
16. [Harness：AI 交付工程体系](#16-harnessai-交付工程体系)
17. [基础设施与部署](#17-基础设施与部署)
18. [测试策略](#18-测试策略)
19. [附录：关键数据结构速查](#19-附录关键数据结构速查)
20. [技术选型深度分析](#20-技术选型深度分析)
   - [20.1 核心选型建议与原因](#201-核心选型建议与原因)
   - [20.2 技术扩展点](#202-技术扩展点按优先级排序)
   - [20.3 面试高频追问点](#203-面试高频追问点)

---

## 1. 系统技术架构

### 1.1 分层架构全景图

系统采用**六层分层架构**，每层职责清晰、边界明确，层与层之间通过接口/消息/事件解耦：

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                    展示层 (Presentation Layer)                             │
│                                                                                         │
│   ┌──────────────────────────────────────┐    ┌──────────────────────────────────────┐  │
│   │          Web UI (React SPA)          │    │        Grafana Dashboards            │  │
│   │  ┌──────┬──────┬──────┬──────────┐  │    │  ┌────────┬────────┬────────────┐    │  │
│   │  │ Chat │Trace │Metrics│Admin     │  │    │  │ Agent  │ RAG    │ Infrastructure│  │  │
│   │  │Panel │Panel │Panel │Upload    │  │    │  │Overview│Quality │ Status       │  │  │
│   │  └──────┴──────┴──────┴──────────┘  │    │  └────────┴────────┴────────────┘    │  │
│   │  Vite + Tailwind v4 + shadcn/ui     │    │  Prometheus Data Source              │  │
│   └──────────────────┬───────────────────┘    └──────────────────┬───────────────────┘  │
│                      │  HTTP/REST                              │  HTTP                 │
├──────────────────────┼─────────────────────────────────────────┼───────────────────────┤
│                      ▼                                         ▼                       │
│                                 接口层 (API Layer)                                       │
│                                                                                         │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐  │
│   │                        FastAPI + Uvicorn (ASGI Server)                           │  │
│   │                                                                                 │  │
│   │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────┐    │  │
│   │  │ POST     │  │ GET      │  │ POST     │  │ GET      │  │ GET /prometheus│    │  │
│   │  │ /chat    │  │ /metrics │  │ /feedback│  │ /health  │  │ _metrics      │    │  │
│   │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └───────┬───────┘    │  │
│   │       │             │             │             │               │             │  │
│   │  ┌────┴─────────────┴─────────────┴─────────────┴───────────────┴────┐        │  │
│   │  │                         Middleware Stack                           │        │  │
│   │  │  TenantMiddleware → RateLimiter → CORSMiddleware                   │        │  │
│   │  └────────────────────────────────────────────────────────────────────┘        │  │
│   └──────────────────────────────────────┬──────────────────────────────────────────┘  │
│                                          │                                             │
├──────────────────────────────────────────┼─────────────────────────────────────────────┤
│                                          ▼                                             │
│                               编排层 (Orchestration Layer)                               │
│                                                                                         │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐  │
│   │                      LangGraph StateGraph (有向无环图引擎)                         │  │
│   │                                                                                 │  │
│   │   ┌────────────────────────── 工作流拓扑 (14 节点) ─────────────────────────┐   │  │
│   │   │                                                                         │   │
│   │   │  START → load_memory → check_permission → classify_intent              │   │  │
│   │   │                ↓                ↓                ↓                      │   │  │
│   │   │           (权限拒绝)       (未知意图)      call_tools ⇄ retrieve        │   │  │
│   │   │                ↓                ↓                ↓                ↓      │   │  │
│   │   │         final_refusal    human_fallback    build_context    rewrite     │   │  │
│   │   │                ↓                ↓                ↓                ↓      │   │  │
│   │   │           save_memory  ←── save_memory  ←─ generate → verify          │   │  │
│   │   │                ↓                ↓           ↓  ↑        ↓  ↑           │   │  │
│   │   │               END              END      finalize   regenerate         │   │  │
│   │   │                                                                         │   │  │
│   │   └─────────────────────────────────────────────────────────────────────────┘   │  │
│   │                                                                                 │  │
│   │   状态管理: PipelineState (TypedDict, ~70 字段, 贯穿所有节点)                        │  │
│   │   路由决策: 7 个条件路由函数 (基于状态字段动态选择下一节点)                        │  │
│   │   重试机制: retrieve(1次)、tool_call(2次)、verify(1次) 内置循环                   │  │
│   └──────────────────────────────────────┬──────────────────────────────────────────┘  │
│                                          │                                             │
├──────────────────────────────────────────┼─────────────────────────────────────────────┤
│                                          ▼                                             │
│                                 能力层 (Capability Layer)                                │
│                                                                                         │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│   │  功能模块    │  │  RAG 引擎    │  │  工具系统     │  │  记忆 & 上下文系统        │  │
│   │              │  │              │  │              │  │                          │  │
│   │ ┌──────────┐ │  │ ┌──────────┐ │  │ ┌──────────┐ │  │ ┌────────────────────┐   │  │
│   │ │ Router   │ │  │ │Document  │ │  │ │Registry  │ │  │ │ ShortTermMemory    │   │  │
│   │ │ Agent    │ │  │ │Loader    │ │  │ │(5 Tools) │ │  │ │ (Redis LIST+PG)    │   │  │
│   │ ├──────────┤ │  │ ├──────────┤ │  │ ├──────────┤ │  │ ├────────────────────┤   │  │
│   │ │Knowledge │ │  │ │Preprocess│ │  │ │Executor  │ │  │ │ SummaryMemory      │   │  │
│   │ │Agent     │ │  │ │& Splitter│ │  │ │(安全校验) │ │  │ │ (PG sessions+Redis)│   │  │
│   │ ├──────────┤ │  │ ├──────────┤ │  │ ├──────────┤ │  │ ├────────────────────┤   │  │
│   │ │Tool      │ │  │ │BGE-M3    │ │  │ │Policy    │ │  │ │ UserMemory         │   │  │
│   │ │Agent     │ │  │ │Embedding │ │  │ │Engine    │ │  │ │ (PG users+Mock)    │   │  │
│   │ ├──────────┤ │  │ ├──────────┤ │  │ ├──────────┤ │  │ ├────────────────────┤   │  │
│   │ │Verifier  │ │  │ │Milvus    │ │  │ │Audit     │ │  │ │ CheckpointStore    │   │  │
│   │ │Agent     │ │  │ │VectorStore│ │  │ │Logger    │ │  │ │ (Redis KV)         │   │  │
│   │ └──────────┘ │  │ ├──────────┤ │  │ └──────────┘ │  │ ├────────────────────┤   │  │
│   │              │  │ │Hybrid    │ │  │              │  │ │ TokenBudget        │   │  │
│   │ 规则+LLM    │  │ │Retriever │ │  │ Safe/        │  │ │ (优先级分配+截断)  │   │  │
│   │ + Rules      │  │ │(向量+BM25)│ │  │ Sensitive/   │  │ ├────────────────────┤   │  │
│   │ Fallback     │  │ ├──────────┤ │  │ Dangerous    │  │ │ CitationManager    │   │  │
│   │              │  │ │MinIO     │ │  │ 三级安全     │  │ │ (引用生成+格式化)  │   │  │
│   │              │  │ │DocStore  │ │  │              │  │ ├────────────────────┤   │  │
│   │              │  │ └──────────┘ │  │              │  │ │ PromptBuilder      │   │  │
│   │              │  │              │  │              │  │ │ (三Agent Prompt)   │   │  │
│   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘  │  │
│          │                 │                 │                     │              │  │
│   ┌──────┴─────────────────┴─────────────────┴─────────────────────┴──────┐       │  │
│   │                        横切关注点 (Cross-cutting)                       │       │  │
│   │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────┐ │       │  │
│   │  │ RecoveryManager  │  │    Tracer        │  │   Eval System        │ │       │  │
│   │  │ (故障分级恢复)    │  │ (全链路事件追踪)  │  │ (离线+在线+Flywheel) │ │       │  │
│   │  └──────────────────┘  └──────────────────┘  └──────────────────────┘ │       │  │
│   └───────────────────────────────────────────────────────────────────────┘       │  │
│   └──────────────────────────────────────┬──────────────────────────────────────────┘  │
│                                          │                                             │
├──────────────────────────────────────────┼─────────────────────────────────────────────┤
│                                          ▼                                             │
│                                 模型层 (Model Layer)                                     │
│                                                                                         │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐  │
│   │                          LLM Provider 抽象层                                      │  │
│   │                                                                                 │  │
│   │  ┌─────────────────┐  ┌─────────────────────────┐  ┌─────────────────────────┐ │  │
│   │  │  MockProvider   │  │ OpenAICompatibleProvider │  │  DashScopeProvider      │ │  │
│   │  │  (零依赖)        │  │  (OpenAI/vLLM/Ollama)   │  │  (通义千问)              │ │  │
│   │  │                 │  │                         │  │                         │ │  │
│   │  │  返回确定性      │  │  AsyncOpenAI client     │  │  dashscope.Generation   │ │  │
│   │  │  mock 响应       │  │  API Key + Base URL     │  │  API Key + Model        │ │  │
│   │  │                 │  │  temperature/max_tokens  │  │  temperature/max_tokens  │ │  │
│   │  └────────┬────────┘  └────────────┬────────────┘  └────────────┬────────────┘ │  │
│   │           │                        │                            │              │  │
│   │           └────────────────────────┼────────────────────────────┘              │  │
│   │                                    │                                           │  │
│   │                      ┌─────────────▼─────────────┐                             │  │
│   │                      │    ProviderFactory        │                             │  │
│   │                      │  LLM_PROVIDER 环境变量    │                             │  │
│   │                      │  统一接口: generate()     │                             │  │
│   │                      │  自动降级: 真实→Mock      │                             │  │
│   │                      └───────────────────────────┘                             │  │
│   └─────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                         │
│   ┌─────────────────────────────────────────────────────────────────────────────────┐  │
│   │                          本地 Embedding 模型                                      │  │
│   │                                                                                 │  │
│   │  ┌─────────────────────────┐  ┌─────────────────────────────────────────────┐  │  │
│   │  │  BGE-M3 (Primary)       │  │  RandomEmbeddingProvider (Fallback)           │  │  │
│   │  │  SentenceTransformer    │  │  无需模型文件，返回随机向量                    │  │  │
│   │  │  1024 维向量            │  │  保证系统在无 GPU/模型时仍可运行               │  │  │
│   │  │  中英双语支持            │  │                                              │  │  │
│   │  └─────────────────────────┘  └─────────────────────────────────────────────┘  │  │
│   └─────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                         │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                         │
│                               基础设施层 (Infrastructure Layer)                           │
│                                                                                         │
│   ┌─────────────────────── 数据平面 (Data Plane) ───────────────────────────────────┐   │
│   │                                                                                 │   │
│   │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────────────┐  │   │
│   │  │   PostgreSQL 16  │  │     Redis 7      │  │       Milvus v2.4.0          │  │   │
│   │  │                  │  │                  │  │                              │  │   │
│   │  │  • 用户信息       │  │  • 会话缓存       │  │  • 向量存储 (HNSW 索引)       │  │   │
│   │  │  • 会话/消息      │  │  • 工具状态       │  │  • 相似度搜索 (IP/COSINE)    │  │   │
│   │  │  • 审计日志       │  │  • Rate Limit    │  │  • 元数据过滤                │  │   │
│   │  │  • 评估数据       │  │  • Checkpoint    │  │  • 集合管理                  │  │   │
│   │  │  • 用户反馈       │  │  • Pending Conf. │  │                              │  │   │
│   │  │  :5432            │  │  :6379           │  │  :19530                      │  │   │
│   │  └──────────────────┘  └──────────────────┘  └──────────────────────────────┘  │   │
│   │                                                                                 │   │
│   │  ┌──────────────────────────────────────────────────────────────────────────┐  │   │
│   │  │                 Elasticsearch 8.17 + IK Analyzer                          │  │   │
│   │  │  • 全文关键词检索 (BM25)                                                   │  │   │
│   │  │  • IK 中文分词 (ik_max_word / ik_smart)                                   │  │   │
│   │  │  • 混合检索融合 (RRF with Milvus)                                         │  │   │
│   │  │  :9200                                                                     │  │   │
│   │  └──────────────────────────────────────────────────────────────────────────┘  │   │
│   │                                                                                 │   │
│   │  ┌──────────────────────────────────────────────────────────────────────────┐  │   │
│   │  │                          MinIO (S3-Compatible)                            │  │   │
│   │  │  • 原始文档存储 (.md/.txt/.pdf/.docx)                                     │  │   │
│   │  │  • 离线评估文件                                                            │  │   │
│   │  │  • 上传文件管理                                                            │  │   │
│   │  │  :9000 (API) / :9001 (Console)                                            │  │   │
│   │  └──────────────────────────────────────────────────────────────────────────┘  │   │
│   └─────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                         │
│   ┌─────────────────────── 可观测性平面 (Observability Plane) ──────────────────────┐   │
│   │                                                                                 │   │
│   │  ┌────────────────────────┐    ┌──────────────────┐    ┌──────────────────┐    │   │
│   │  │ OpenTelemetry Collector│    │   Prometheus      │    │  Grafana 11.5    │    │   │
│   │  │                        │    │                   │    │                  │    │   │
│   │  │  • Trace 采集          │───▶│  • 指标抓取+存储   │───▶│  • 可视化面板     │    │   │
│   │  │  • Metric 转发         │    │  • TSDB 时序数据库 │    │  • 告警规则      │    │   │
│   │  │  • Log 管线            │    │  • PromQL 查询     │    │  • 数据源管理    │    │   │
│   │  │  :4317(gRPC):4318(HTTP)│    │  :9090             │    │  :3000           │    │   │
│   │  └────────────────────────┘    └──────────────────┘    └──────────────────┘    │   │
│   └─────────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心数据流 (Request Lifecycle)

一次完整的用户问答请求的生命周期，穿越全部六层架构：

```
时间线 ──────────────────────────────────────────────────────────────────────────────▶

  用户                      API层                   编排层                   能力层+模型层
  │                         │                       │                         │
  │  ① POST /chat          │                       │                         │
  │  {query,user_id,...}   │                       │                         │
  ├────────────────────────▶│                       │                         │
  │                         │  ② 中间件处理          │                         │
  │                         │  Tenant → RateLimit    │                         │
  │                         │  ③ new_trace()        │                         │
  │                         │  trace_id = "a1b2...f6"│                        │
  │                         ├───────────────────────▶│                         │
  │                         │                       │  ④ graph.ainvoke()      │
  │                         │                       │  state = PipelineState()   │
  │                         │                       │                         │
  │                         │                       │  ┌─ load_memory ────────▶ MemoryManager
  │                         │                       │  │  加载会话历史/摘要/档案  ◀── Redis + PG
  │                         │                       │  │                       │
  │                         │                       │  ├─ check_permission ───▶ PermissionTool
  │                         │                       │  │  校验 knowledge_search  │
  │                         │                       │  │                       │
  │                         │                       │  ├─ classify_intent ─────▶ Router Module
  │                         │                       │  │  关键词匹配 / LLM分类   │  ◀── LLM Provider
  │                         │                       │  │                       │
  │                         │                       │  ├─ [call_tools] ────────▶ Tool Module
  │                         │                       │  │  安全校验 → 执行工具    │  ◀── ToolExecutor
  │                         │                       │  │                       │
  │                         │                       │  ├─ retrieve_knowledge ──▶ Retriever
  │                         │                       │  │  BGE-M3编码 → Milvus   │  ◀── VectorStore
  │                         │                       │  │  混合检索 → 重排序      │
  │                         │                       │  │                       │
  │                         │                       │  ├─ build_context ───────▶ ContextManager
  │                         │                       │  │  Token预算 → 截断      │
  │                         │                       │  │  引用生成 → Prompt组装  │
  │                         │                       │  │                       │
  │                         │                       │  ├─ generate_answer ─────▶ Knowledge Module
  │                         │                       │  │  文档拼接 → LLM生成     │  ◀── LLM Provider
  │                         │                       │  │                       │
  │                         │                       │  ├─ verify_answer ───────▶ Verifier Module
  │                         │                       │  │  规则检查 / LLM校验     │  ◀── LLM Provider
  │                         │                       │  │  ├─通过→finalize       │
  │                         │                       │  │  └─失败→regenerate     │
  │                         │                       │  │                       │
  │                         │                       │  └─ save_memory ─────────▶ MemoryManager
  │                         │                       │     保存对话/摘要/检查点    ▶ Redis + PG
  │                         │                       │                         │
  │                         │  ←── ChatResponse ────│  ⑤ 组装最终响应          │
  │  ⑥ 展示答案+引用+追踪   │◀──────────────────────│                         │
  │  ←──────────────────────│                       │                         │
  │                         │                       │                         │
  │  ⑦ 用户反馈 👍/👎       │                       │                         │
  ├────────────────────────▶│                       │                         │
  │                         │  ⑧ POST /feedback    │                         │
  │                         │  → PG feedback 表     │                         │
  │                         │  → failed_cases.jsonl │                         │
  │                         │  → Data Flywheel      │                         │
```

### 1.3 组件交互矩阵

展示核心组件之间的调用关系和数据流向：

```
                        ┌─────────────────────────────────────────────────────────────┐
                        │                    build_workflow()                           │
                        │                   (应用组装入口)                               │
                        └──────────────────────┬──────────────────────────────────────┘
                                               │ 创建并连接
                ┌──────────────────────────────┼──────────────────────────────┐
                │                              │                              │
                ▼                              ▼                              ▼
        ┌──────────────┐             ┌─────────────────┐            ┌─────────────────┐
        │  Retriever   │             │  MemoryManager  │            │  ContextManager │
        │ chunk=500    │             │                 │            │  max_tokens=4096│
        │ top_k=5      │             │ ┌─────────────┐ │            │                 │
        └──────┬───────┘             │ │ShortTerm 10 │ │            │ ┌─────────────┐ │
               │                     │ │Summary 6轮   │ │            │ │TokenBudget  │ │
    ┌──────────┼──────────┐          │ │User profiles│ │            │ │CitationMgr  │ │
    │          │          │          │ │Checkpoint   │ │            │ │PromptBuilder│ │
    ▼          ▼          ▼          │ └─────────────┘ │            │ └─────────────┘ │
┌──────┐ ┌──────┐ ┌──────────┐      └────────┬────────┘            └────────┬────────┘
│BGE-M3│ │BM25  │ │ Milvus   │               │                              │
│Embed │ │关键词 │ │ Vector   │               │ 记忆读写                      │ 预算+引用+Prompt
│      │ │检索  │ │ Store    │               │                              │
└──┬───┘ └──┬───┘ └────┬─────┘               │                              │
   │        │          │                      │                              │
   └────────┼──────────┘                      │                              │
            │ 融合排序                         │                              │
            ▼                                 │                              │
    ┌──────────────┐                          │                              │
    │ retrieved_docs                         │                              │
    │ (向量+关键词)│                          │                              │
    └──────────────┘                          │                              │
            │                                 │                              │
            └────────────┬────────────────────┘                              │
                         │                                                   │
                         ▼                                                   ▼
                ┌──────────────────────────────────────────────────────────────────┐
                │                          PipelineState                               │
                │                    (全局共享状态字典)                               │
                │                                                                  │
                │  query │ intent │ retrieved_docs │ draft_answer │ verified │ ...  │
                │                                                                  │
                └──────────────────────────────────────────────────────────────────┘
                         │                     │                     │
                         ▼                     ▼                     ▼
                ┌──────────────┐     ┌──────────────┐      ┌──────────────┐
                │ Router Module  │     │Knowledge Module│      │ Verifier Module│
                │              │     │              │      │              │
                │ classify_    │     │ generate_    │      │ verify_      │
                │ intent()     │     │ answer()     │      │ answer()     │
                └──────┬───────┘     └──────┬───────┘      └──────┬───────┘
                       │                    │                      │
                       └────────────────────┼──────────────────────┘
                                            │
                                   ┌────────▼────────┐
                                   │  ProviderFactory │
                                   │  get_llm_provider│
                                   └────────┬────────┘
                                            │
                          ┌─────────────────┼─────────────────┐
                          ▼                 ▼                 ▼
                   ┌────────────┐  ┌──────────────┐  ┌────────────┐
                   │MockProvider│  │OpenAICompat  │  │DashScope   │
                   │(默认)      │  │Provider      │  │Provider    │
                   └────────────┘  └──────────────┘  └────────────┘
```

### 1.4 部署架构 (Deployment Topology)

```
                              ┌─────────────────────────────┐
                              │      用户 / 客户端            │
                              │  Browser (localhost:5173)    │
                              └──────────────┬──────────────┘
                                             │
                              ┌──────────────┴──────────────┐
                              │     Nginx / Vite Proxy      │
                              │  /api/* → backend:8000      │
                              └──────────────┬──────────────┘
                                             │
        ┌────────────────────────────────────┼────────────────────────────────────┐
        │                            Docker Network (earg-net)                     │
        │                                                                         │
        │  ┌─────────────────────────────┐                                        │
        │  │   App Container (Python)    │  开发模式:                              │
        │  │   FastAPI + Uvicorn         │  .venv/bin/uvicorn                     │
        │  │   :8000                     │  --reload --port 8000                  │
        │  └─────────────┬───────────────┘                                        │
        │                │                                                        │
        │    ┌───────────┼───────────┬───────────┬───────────┬──────────┬──────────┬──────────┐
        │    │           │           │           │           │          │          │          │
        │    ▼           ▼           ▼           ▼           ▼          ▼          ▼          ▼
        │ ┌──────┐ ┌──────┐ ┌──────────┐ ┌──────┐ ┌──────────┐ ┌──────┐ ┌──────────┐ ┌──────┐
        │ │  PG  │ │Redis │ │  Milvus  │ │MinIO │ │    ES    │ │Neo4j │ │  OTEL    │ │Prom  │
        │ │ :5432│ │:6379 │ │ :19530   │ │:9000 │ │  :9200   │ │:7687 │ │:4317/4318│ │ :9090│
        │ └──────┘ └──────┘ └──────────┘ └──────┘ └──────────┘ └──────┘ └─────┬────┘ └──┬───┘
        │                                                                      │         │
        │                                                                      └────┬────┘
        │                                                                           │
        │                                                                           ▼
        │                                                                    ┌──────────┐
        │                                                                    │ Grafana  │
        │                                                                    │  :3000   │
        │                                                                    └──────────┘
        └─────────────────────────────────────────────────────────────────────────┘

        存储卷 (Docker Volumes):
        ┌──────┬───────┬──────────┬───────┬──────┬───────┬─────────┬──────────┐
        │pgdata│redisda│milvusdata│minioda│esdata│promdat│grafana  │neo4j_data│
        │      │ta     │          │ta     │      │a      │-data    │          │
        └──────┴───────┴──────────┴───────┴──────┴───────┴─────────┴──────────┘

        降级路径 (当 Docker 服务不可用时):
        ┌─────────────────┬───────────────────────────┐
        │  PostgreSQL     │ → 内存 dict/Mock          │
        │  Redis          │ → 内存 dict/deque         │
        │  Milvus         │ → MemoryVectorStore       │
        │  Elasticsearch  │ → Jaccard 内存关键词匹配   │
        │  Neo4j          │ → 自动退回 hybrid_only    │
        │  MinIO          │ → 本地文件系统             │
        │  OTEL           │ → JSONL 文件              │
        │  Prometheus     │ → MetricsCollector        │
        │  Grafana        │ → 不可用 (无面板)          │
        └─────────────────┴───────────────────────────┘
```

### 1.5 安全架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            安全边界 (Security Perimeter)                      │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                          请求入口                                    │   │
│   │                                                                     │   │
│   │  ① 租户隔离 ──▶ X-Tenant-ID 提取 → request.state.tenant_id         │   │
│   │  ② 速率限制 ──▶ Redis 滑动窗口 → 100 req/min/user                   │   │
│   │  ③ CORS     ──▶ 允许来源白名单                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                          权限门控                                    │   │
│   │                                                                     │   │
│   │  check_permission ──▶ knowledge_search in permissions?              │   │
│   │                            │                                         │   │
│   │                    ┌───────┴───────┐                                 │   │
│   │                    │ YES           │ NO                              │   │
│   │                    ▼               ▼                                 │   │
│   │              继续工作流      final_refusal                           │   │
│   │                             "抱歉，权限不足"                          │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                          工具安全分级                                │   │
│   │                                                                     │   │
│   │  ┌────────────┐    ┌────────────────┐    ┌──────────────────┐       │   │
│   │  │ Safe 工具   │    │ Sensitive 工具  │    │ Dangerous 工具    │       │   │
│   │  │            │    │                │    │                  │       │   │
│   │  │ 无需确认   │    │ 自动注入user_id│    │ 需用户确认        │       │   │
│   │  │ 最小审计   │    │ 标准审计       │    │ 完整审计          │       │   │
│   │  │ 不限频     │    │ 10 req/min     │    │ 3 req/min         │       │   │
│   │  │            │    │                │    │                  │       │   │
│   │  │ system_    │    │ query_ticket   │    │ create_ticket    │       │   │
│   │  │ status     │    │ get_user_      │    │                  │       │   │
│   │  │ error_code │    │ profile        │    │                  │       │   │
│   │  └────────────┘    └────────────────┘    └──────────────────┘       │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                          数据安全                                    │   │
│   │                                                                     │   │
│   │  • input_summary 自动截断 (≤120字符)，不记录完整 query 或文档内容    │   │
│   │  • 审计日志写入 PostgreSQL audit_logs 表 (不可篡改)                  │   │
│   │  • 敏感 tool params 在序列化前脱敏                                   │   │
│   │  • 人工兜底载荷不包含系统内部状态 (仅业务上下文)                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.6 关键技术决策记录 (ADR)

| 决策点 | 方案 | 备选方案 | 决策理由 |
|--------|------|----------|----------|
| **工作流引擎** | LangGraph StateGraph | LangChain Chain / 自研 DAG | LangGraph 原生支持条件分支+循环+状态持久化，社区活跃 |
| **向量数据库** | Milvus 2.4 Standalone | Qdrant / Chroma / pgvector | 企业级 HNSW 索引性能、元数据过滤、水平扩展能力；系统中同时保留 Qdrant 可选替代 |
| **全文检索** | Elasticsearch 8 + IK Analyzer | 纯内存检索 / Whoosh | BM25 专业全文检索、IK 中文分词质量高、索引持久化 |
| **图数据库** | Neo4j 5.x | NebulaGraph / ArangoDB | 生态成熟、Cypher 语法简洁、5.x 内存优化好、社区资源丰富 |
| **嵌入模型** | BGE-M3 (本地) | OpenAI text-embedding-3 / Cohere | 中英双语、本地零成本、支持 dense+sparse 混合 |
| **LLM 策略** | 规则驱动 + LLM 可选增强 | 纯 LLM / 纯规则 | 规则路径始终可用作为保底，LLM 仅在同步上下文且配置真实 API key 时作为可选增强 |
| **记忆存储** | Redis + PG + 内存三级 | 纯 Redis / 纯 PG | 多层写入保证数据不丢，任意层挂掉不影响系统 |
| **状态管理** | TypedDict (PipelineState) | Pydantic BaseModel / dataclass | LangGraph 原生兼容，reducer 机制支持增量更新 |
| **前端框架** | React + Vite + Tailwind v4 | Next.js / Vue + Nuxt | 轻量 SPA，Vite HMR 开发体验好，shadcn/ui 组件丰富 |
| **可观测性** | 自研 + JSONL (当前) | LangSmith / OpenTelemetry | 当前 Mock 阶段自研零依赖，预留了 OTel 集成路径 |
| **评估体系** | 自研 (离线+在线) | RAGAS 完整集成 | 当前规则 + LLM-Judge 足够覆盖核心指标，渐进式增强 |
| **检索路由** | 纯规则引擎 (9条规则) | LLM 动态路由 | 零延迟、确定性输出、可测试、每条决策写可读 reason；规则覆盖关系推理/精确匹配/语义理解三类场景 |
| **图检索编排** | graph_first / parallel 双模式 + 降级链 | 全 LLM Agent 自主决策 | 规则级路由决定 mode，编排器只负责执行；graph 失败自动降级且完整记录到 trace |

---

## 2. 项目总览与设计哲学

### 2.1 核心定位

Enterprise Hybrid RAG 是一个**企业级混合 RAG 问答系统**（规则驱动 + LLM 可选增强），核心目标是在企业内部知识库场景中提供**可靠、可观测、可恢复、可评估**的 AI 问答能力。LLM 调用为可选增强层，系统在未配置 LLM 或 async 上下文下仍可完整运行。

与简单的 "embedding + LLM" 方案不同，本项目从一开始就将**工程化交付治理**作为一等公民，实现了从 RAG 运行时到 CI/CD、质量门禁、灰度发布、回滚的完整闭环。

### 2.2 设计原则

本项目遵循以下核心设计原则：

| 原则 | 说明 | 实现方式 |
|------|------|----------|
| **优雅降级 (Graceful Degradation)** | 任何外部依赖不可用时系统不崩溃 | 全局 fallback 到内存 mock 实现 |
| **防御性深度 (Defense in Depth)** | 多层校验，不信任任何单一环节 | 检索评分→文档过滤→答案校验→最终兜底 |
| **可观测性优先 (Observability First)** | 每个关键操作都有追踪 | JSONL 事件日志 + Prometheus 指标 + 结构化追踪 |
| **故障可恢复 (Recoverability)** | 失败可重试、可降级、可升级 | 分级恢复策略：重试→降级→人工兜底 |
| **LLM 无关 (LLM-Agnostic)** | 不绑定特定 LLM 提供商 | 抽象 LLM Provider 层，支持 mock/openai/dashscope |
| **渐进式复杂度 (Progressive Complexity)** | 简单场景走快速路径，复杂场景走增强路径 | 关键词 fallback + LLM 增强双路径 |

### 2.3 技术栈全景图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端 (React + TypeScript)                      │
│  Vite + Tailwind CSS v4 + shadcn/ui + lucide-react                   │
├─────────────────────────────────────────────────────────────────────┤
│                       API 层 (FastAPI + Uvicorn)                      │
│  /chat  /feedback  /metrics  /health  /prometheus_metrics            │
├─────────────────────────────────────────────────────────────────────┤
│                    LangGraph 工作流引擎 (14 节点)                      │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐           │
│  │  Router  │Knowledge │  Tool    │ Verifier │ Recovery │           │
│  │  Agent   │  Agent   │  Agent   │  Agent   │  Manager │           │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘           │
├─────────────────────────────────────────────────────────────────────┤
│         RAG 引擎       │  记忆系统    │  上下文管理  │  工具系统       │
│  DocumentLoader        │ ShortTerm   │ TokenBudget  │ ToolRegistry   │
│  TextSplitter          │ Summary     │ CitationMgr  │ ToolExecutor   │
│  EmbeddingProvider     │ UserMemory  │ PromptBuilder│ ToolPolicy     │
│  Retriever (三路融合)   │ Checkpoint  │ ContextMgr   │ Adapters       │
│  GraphRAG Orchestrator │              │              │                │
├─────────────────────────────────────────────────────────────────────┤
│                       LLM Provider 抽象层                            │
│  MockProvider │ OpenAICompatibleProvider │ DashScopeProvider         │
├─────────────────────────────────────────────────────────────────────┤
│                   数据持久化 & 中间件                                  │
│  Storage (SQLAlchemy) │ Middleware (Tenant + RateLimiter)            │
├─────────────────────────────────────────────────────────────────────┤
│                       基础设施 (Docker Compose 9 服务)                │
│  PostgreSQL │ Redis │ Milvus │ Elasticsearch │ Neo4j │ MinIO         │
│  OpenTelemetry Collector │ Prometheus │ Grafana                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

